#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main() {
  int n;
  while(cin >> n, n) {
    map<int, int[3]> m;
    int i, p, q, r;
    cin >> i >> p >> q >> r;
    m.insert(make_pair(i);
  }

}
